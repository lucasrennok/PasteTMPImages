import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

//ReactDom render components at 'root'
ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);

